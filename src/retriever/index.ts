/**
 * Tool retriever module exports.
 *
 * Provides full-text search capabilities for discovering tools
 * across multiple upstream MCP servers.
 *
 * @module retriever
 */

export {
  type RetrieveResult,
  Retriever,
  type RetrieverOptions,
  type SearchOptions,
  type ToolFullSchema,
  type ToolIdentity,
  type ToolResult,
  type ToolSummary,
} from "./retriever.js";
